model {
	for (i in 1:N) {
	  Y[i] ~ dnorm(mu[i], tau2Sp / delta2)
		mu[i] <- inprod(X[i,], beta) + phi[i]
		yFit[i] ~ dnorm(mu[i], tau2Sp / delta2) ##Posterior predictive model fitwwww
	}
  phi[1:N] ~ dcar_proper(zeros, C, adj, num, M, tau2Sp, gamma)
	for (i in 1:p) {
	  beta[i] ~ dnorm(0.0, tau2_beta)
	}
	  sigma2 <- 1 / tau2
	  sigma2Sp <- 1 / tausqSp
	  delta2 <- sigma2Sp / sigma2
    tau2 ~ dgamma(a0_tau2, b0_tau2)
    delta2 ~ dbeta(a0_delta2, b0_delta2)
    gamma ~ dunif(-1, 1)
}
